

var isHotOutside = true; 
var isWeekDay = true;
var hasMoneyInPocket = true;

var costOfMilk = 5.00;
var moneyInWallet = 20.00;
var thirstLevel = 7;

var shouldByIceCream = true;
if (isHotOutside && moneyInWallet);
console.log (shouldByIceCream);

var willGoSwimming = true; 
if (isHotOutside && !isWeekDay);
console.log(willGoSwimming);

var isAGoodDay = true;
if (isHotOutside && moneyInWallet && !isWeekDay);
console.log(isAGoodDay)

var willBuyMilk = true;
if (isHotOutside && thirstLevel >= costOfMilk *2);
console.log(willBuyMilk);


